#include <iostream>
#include <vector>

using namespace std;

class Solution {
    vector<int> merge_sort(vector<int> input)
    {   
        int len = input.size();
        vector<int>  result(len, 0);
        if (len < 1)
            return result;
        
        vector<int> temp = input;
        int count = len / 2;
        int i = 0;
        int stride = 1;
        while (i < count)
        {
            result
        }
        
    }
};

int main()
{

    return 0;
}
